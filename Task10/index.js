// Given the object { name: 'Alice', age: 25, city: 'London' },
// use destructuring to extract and print the name and age.

const obj = { name: "Alice", age: 25, city: "London" };
const { name, age } = obj;
console.log(name);
console.log(age);
